# Environment Configuration
# Copy this file to .env and update the values

# Your server's IP address (update this before evaluation)
# Find your IP with: ipconfig (Windows) or ifconfig (Linux/Mac)
# Examples:
#   - University WiFi: HOST_IP=192.168.5.100
#   - Home WiFi: HOST_IP=10.0.0.50
#   - Auto-detect: HOST_IP=auto
HOST_IP=auto

# Flask environment
FLASK_ENV=development

# MongoDB connection (already configured in docker-compose.yml)
# MONGO_URI=mongodb://mongo:27017/cp_db
